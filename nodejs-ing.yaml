apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: nodejs-ingress
spec:
  rules:
  - http:
      paths:
      - path: /users
        backend:
          serviceName: nodejs-svc
          servicePort: 80
